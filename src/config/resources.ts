const INCIDENTS = `${window.location.href}data/crime_data.json`;
const BOUNDARY = `${window.location.href}data/City_of_Boston_Boundary.geojson`;
const NEIGHBORHOODS = `${window.location.href}data/Boston_Neighborhoods.geojson`;
const POLICE_STATIONS = `${window.location.href}data/Boston_Police_Stations.geojson`;
const POLICE_DISTRICTS = `${window.location.href}data/Police_Districts.geojson`;
const VISION_ZERO = `${window.location.href}data/vision_zero_ss.json`;

export {
  INCIDENTS,
  BOUNDARY,
  NEIGHBORHOODS,
  POLICE_STATIONS,
  POLICE_DISTRICTS,
  VISION_ZERO,
};
